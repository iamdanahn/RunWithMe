    <% if logged_in? %>
      <script>
        window.currentUser = {
        "id": <%= current_user.id %>,
        "first_name": "<%= current_user.first_name %>",
        "last_name": "<%= current_user.last_name %>",
        "email": "<%= current_user.email %>",
        "created_at": "<%= current_user.created_at %>",
        };
      </script>
    <% end %> 
    
<main id="root">React did not load</main>
